# Acceptance Spec

## Критерии готовности MVP

Проект считается готовым к учебной сдаче, если выполняются все пункты:

- `npm install` проходит.
- `npm run lint` проходит.
- `npm run test` проходит.
- `npm run build` проходит.
- `.env.example` содержит все переменные.
- `.env.local` существует локально и не коммитится.
- Supabase migration описывает все таблицы.
- Telegram-бот принимает voice message.
- STT возвращает raw text.
- LLM cleanup возвращает читаемый текст.
- LLM parser возвращает строгий JSON.
- Запись сохраняется в `voice_records`.
- Продажа сохраняется в `sales`.
- Позиции сохраняются в `sale_items`.
- Позиции без цены получают `needs_price`.
- `/daily-report` показывает товар, количество и выручку.
- Одинаковые товары группируются.
- Итоговая сумма считается только по известным ценам.
- Блок “Нужно проверить” показывает спорные позиции.
- README объясняет запуск.
- Документация организована по разделам.
- Секретов в коде нет.

## Демо-проверка

1. Запустить проект.
2. Отправить голосовое: `хлеб 3 по 40 молоко 2 по 90`.
3. Открыть `/records`.
4. Проверить, что запись видна.
5. Открыть `/daily-report`.
6. Проверить таблицу:
   - Хлеб, 3 шт, 120 ₽.
   - Молоко, 2 шт, 180 ₽.
   - Итого, 5, 300 ₽.
7. Отправить голосовое без цены: `чай 2 штуки`.
8. Проверить, что чай попал в “Нужно проверить”.
