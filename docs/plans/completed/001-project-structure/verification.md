# Структура проекта — проверка

- Проверялись workspace scripts и TypeScript configs.
- Команды: `npm install`, `npm run lint`, `npm run test`, `npm run build`.
- Готово, если все workspaces обнаружены и build завершается без ошибок.
