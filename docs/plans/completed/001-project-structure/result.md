# Структура проекта — результат

Bot, Web App и общая бизнес-логика собираются независимо, но используют единые типы и команды из корневого `package.json`. Структура пригодна для Vercel deploy веб-приложения и локального запуска bot polling.
