# ИИ-парсер — проверка

- Unit: `tests/sale-parser.test.ts`, `tests/records.test.ts`.
- Команда: `npm run test`.
- Готово, если неоднозначные данные не попадают в выручку, а multi-item message не теряет позиции.
