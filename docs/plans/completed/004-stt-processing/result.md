# Обработка STT — результат

Whisper-compatible STT получает подготовленный audio buffer и возвращает текст. Если ffmpeg недоступен, pipeline использует безопасный fallback исходного OGG вместо падения webhook.
