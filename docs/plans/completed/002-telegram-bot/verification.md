# Telegram-бот — проверка

- Unit: `tests/bot.test.ts`.
- Команды: `npm run test`, `npm run bot:dev` при заполненном `.env.local`.
- Готово, если `/start` отвечает, voice доходит до handler, а ошибка возвращает безопасное сообщение.
