# 001 — стабилизация sales flow

Закрыто: базовая изоляция магазина, report calculation, soft delete поля, seller access, parser fallback и regression tests.

Актуальный статус изменён последующими планами: уверенные voice-позиции теперь могут сразу сохраняться как `processed`, а fallback Web App режим поддерживается явно.
