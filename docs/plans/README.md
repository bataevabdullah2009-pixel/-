# Планы

Текущая работа находится в `active`, завершённые планы — в `completed`. План описывает ограниченный результат, checklist и acceptance conditions. Изменение продуктового объёма требует нового плана.
